// bind_addr = "0.0.0.0"
addresses {
  # No default, falls back to bind_addr
  // http = "0.0.0.0"
  // rpc = "0.0.0.0"
  // serf = "0.0.0.0"
}

advertise {
  # Defaults to the first private IP address.
  // http = "0.0.0.0"
  // rpc = "0.0.0.0"
  // serf = "0.0.0.0"
}

ports {
  // http = 4646
  // rpc = 4647
  // serf = 4648
}
